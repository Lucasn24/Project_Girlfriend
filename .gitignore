# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude

# Raw personal chat export and derived data — never commit, contains full private history
src/data/result.json
server/data/reply-pairs.json
server/data/reply-index.json
server/data/calendar-config.json
server/data/calendar-events.json

# OAuth refresh/access tokens — as sensitive as a password, never commit
server/data/google-calendar-tokens.json

# Uploaded/generated couple photos — real photos of both people, never commit
server/data/couple-photo/

# Local generation output for scripts/compare-couple-photo.ts
scratch/